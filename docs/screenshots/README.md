# Screenshots

Drop product screenshots here before the public invite. Filenames referenced by the root README:

- `home.png` — command palette open, session history visible in the sidebar
- `team-pulse.png` — a generated team pulse with inline citations
- `one-on-one.png` — 1:1 prep view showing memory context from prior sessions
- `onboarding.png` — first-run credential setup, privacy consent visible

Capture at 2x resolution on a 1440×900 window. Crop to the app window — no desktop chrome. Prefer real data with names redacted over synthetic fixtures; the point of the screenshots is to show what an actual session looks like.
